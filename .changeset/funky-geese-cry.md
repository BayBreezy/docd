---
"@baybreezy/docd": minor
---

Add `expandNav` config option to control which nav collapsibles are expanded by default. Accepts `true` to expand all, a `number` for a specific 1-based position, or `number[]` for multiple positions.
